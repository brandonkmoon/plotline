# partyserver peers @cloudflare/workers-types@^4 while wrangler peers ^5 — an
# unavoidable transitive conflict. Relax peer resolution so clean installs
# (CI / Vercel) don't fail on it, matching local install behavior.
legacy-peer-deps=true
